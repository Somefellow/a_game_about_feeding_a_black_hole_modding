--- recovered/Global.gd
+++ recovered/Global.gd
@@ -16,6 +16,9 @@
 
 const G = 100000.0
 
+## Mod settings — set DELAY_SPEED to 0.0 for instant auto-play (speedrun)
+const DELAY_SPEED: float = 0.25
+
 var load_saved_run = true
 
 
